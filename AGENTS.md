# jev-agent-skills

**In one line:** Jev is an optional, conservative second opinion for an agent. District: `SISO_Agents` (`~/SISO_Workspace/SISO_Agents/jev-agent-skills`). Owner: the agent stack lead (herdr agent-stack, SISO_Agents/siso-harness-lab).

The rest is in `README.md` if it has one. Find anything else on this machine with `estate where <words>`.
